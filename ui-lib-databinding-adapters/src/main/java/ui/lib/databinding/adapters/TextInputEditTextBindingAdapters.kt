package ui.lib.databinding.adapters

import androidx.databinding.BindingAdapter
import androidx.lifecycle.MutableLiveData
import com.google.android.material.textfield.TextInputEditText
// import ui.lib.databinding.adapter.types.TextBinding

// @BindingAdapter("text")
// fun setTextOrResource(view: TextInputEditText, text: String) {
//     // text.value?.let {
//     //     it.textRes?.let { resId -> view.setText(resId) }
//     //     it.text?.let { str -> view.setText(str) } // text prioritized over resource
//     // }
// }

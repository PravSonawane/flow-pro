<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="ui.feature.flow.steplist.StepListScreenViewModel.ScreenMode" />
        <variable
            name="viewModel"
            type="ui.feature.flow.steplist.StepListScreenViewModel" />
    </data>

    <FrameLayout
        android:id="@+id/container_step_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <include
            android:id="@+id/view_stub_input_step_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            layout="@layout/layout_step_list_mode_input"
            tools:visibility="visible"
            android:visibility="@{viewModel.screenMode == ScreenMode.INPUT_STEP_LIST}"
            app:viewModel="@{viewModel.inputStepListViewModel}" />
    </FrameLayout>
</layout>